{
  "name": "semver-action",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && tsc && npx ncc build dist-ts/index.js -o dist --minify",
    "clean": "node -e \"const fs = require('fs'); ['dist', 'dist-ts'].forEach(d => fs.rmSync(d, { recursive: true, force: true }))\""
  },
  "dependencies": {
    "@actions/core": "^2.0.2",
    "@actions/exec": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.4",
    "eslint": "^10.0.3",
    "prettier": "^3.0.0",
    "rimraf": "^6.1.2",
    "ts-node": "^10.9.2",
    "tslib": "^2.6.3",
    "typescript": "^5.0.0"
  }
}